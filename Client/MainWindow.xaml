<Window x:Class="Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client"
        mc:Ignorable="d"
        Title="Телефончики" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DataGrid Grid.Column="0" Grid.Row="0" AutoGenerateColumns="False" 
                  Margin="0 10 0 0" Name="mainDataGridView"
                  FontSize="16" Grid.ColumnSpan="2" HorizontalAlignment="Center"
                  Width="500">
            <DataGrid.Columns>
                <DataGridTemplateColumn>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Height="25" Width="25" 
                                        Background="Transparent" 
                                        BorderThickness="0" Click="EditClick">
                                    <Image Source="/Resources/edit.png"/>
                                </Button>
                                <Button Height="25" Width="25" 
                                        Background="Transparent" 
                                        BorderThickness="0" Click="BinClick">
                                    <Image Source="/Resources/bin.png"/>
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Название" 
                                    Binding="{Binding Path=Title}"/>

                <DataGridTextColumn Header="Компания" 
                                    Binding="{Binding Path=CompanyEntity.Title}"/>

                <DataGridTextColumn Header="Цена" 
                                    Binding="{Binding Path=Price}"/>
            </DataGrid.Columns>

        </DataGrid>

        <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal">
            <Button x:Name="Add"  Height="80" Width="60" 
                    VerticalAlignment="Stretch" HorizontalAlignment="Left" 
                    Margin="10" Click="AddClick" Background="Transparent">
                <Image Source="/Resources/Plus.png"/>
            </Button>

        </StackPanel>

        <StackPanel Grid.Row="1" Grid.Column="1" >
            <TextBlock Text="Ошибки" Margin="5"/>
            <Border x:Name="ErrorBorder">
                <TextBlock x:Name="ErrorMessage" Foreground="Red" FontWeight="SemiBold"
                           TextWrapping="Wrap" TextAlignment="Center"/>
            </Border>
        </StackPanel>
    </Grid>
</Window>
